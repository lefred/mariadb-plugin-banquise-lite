INCLUDE(CheckIncludeFile)

FIND_PACKAGE(CURL REQUIRED)
FIND_PACKAGE(LibArchive REQUIRED)

SET(BANQUISE_LITE_CRYPTO_BACKEND "AUTO" CACHE STRING
  "Crypto backend: AUTO, OPENSSL, or WOLFSSL")
SET_PROPERTY(CACHE BANQUISE_LITE_CRYPTO_BACKEND PROPERTY STRINGS AUTO OPENSSL WOLFSSL)

IF(BANQUISE_LITE_CRYPTO_BACKEND STREQUAL "AUTO")
  SET(BANQUISE_LITE_USE_WOLFSSL OFF)
  IF(EXISTS "${CMAKE_BINARY_DIR}/include/my_config.h")
    FILE(READ "${CMAKE_BINARY_DIR}/include/my_config.h" BANQUISE_MY_CONFIG)
    IF(BANQUISE_MY_CONFIG MATCHES "#define[ \t]+HAVE_WOLFSSL([ \t]|$)")
      SET(BANQUISE_LITE_USE_WOLFSSL ON)
    ENDIF()
  ENDIF()
  IF(DEFINED SSL_DEFINES AND SSL_DEFINES MATCHES "HAVE_WOLFSSL")
    SET(BANQUISE_LITE_USE_WOLFSSL ON)
  ENDIF()
ELSEIF(BANQUISE_LITE_CRYPTO_BACKEND STREQUAL "WOLFSSL")
  SET(BANQUISE_LITE_USE_WOLFSSL ON)
ELSEIF(BANQUISE_LITE_CRYPTO_BACKEND STREQUAL "OPENSSL")
  SET(BANQUISE_LITE_USE_WOLFSSL OFF)
ELSE()
  MESSAGE(FATAL_ERROR "BANQUISE_LITE_CRYPTO_BACKEND must be AUTO, OPENSSL, or WOLFSSL")
ENDIF()

IF(BANQUISE_LITE_USE_WOLFSSL)
  SET(BANQUISE_LITE_CRYPTO_SOURCE banquise_crypto_wolfssl.cc)
  IF(TARGET wolfssl)
    SET(BANQUISE_LITE_CRYPTO_LIBRARY wolfssl)
  ELSEIF(SSL_LIBRARIES)
    SET(BANQUISE_LITE_CRYPTO_LIBRARY ${SSL_LIBRARIES})
  ELSE()
    MESSAGE(FATAL_ERROR "WolfSSL selected but no MariaDB wolfSSL library is available")
  ENDIF()
ELSE()
  FIND_PACKAGE(OpenSSL REQUIRED COMPONENTS Crypto)
  SET(BANQUISE_LITE_CRYPTO_SOURCE banquise_crypto_openssl.cc)
  SET(BANQUISE_LITE_CRYPTO_LIBRARY OpenSSL::Crypto)
ENDIF()

MYSQL_ADD_PLUGIN(BANQUISE_LITE banquise_lite.cc ${BANQUISE_LITE_CRYPTO_SOURCE}
  MODULE_ONLY
  RECOMPILE_FOR_EMBEDDED
  LINK_LIBRARIES CURL::libcurl LibArchive::LibArchive ${BANQUISE_LITE_CRYPTO_LIBRARY})
